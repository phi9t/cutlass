# Test infrastructure root.

package(default_visibility = ["//tests:__subpackages__"])

cc_library(
    name = "test_main",
    srcs = ["test_main.cc"],
    deps = [
        "@googletest//:gtest",
    ],
)

# Shared test utilities (CPU reference oracles, helpers).
cc_library(
    name = "test_utils",
    hdrs = ["test_utils.h"],
    deps = [
        "//src/tensor",
    ],
)
